"The ``Square`` module
=======================

Using ``Square``
-------------------

First import ``Square`` from the ``square`` file:

    >>> Square = __import__('square').Square

Tests:

    Test - check instantiation with valid arguments
    >>> s = Square(5)
    >>> isinstance(s, Square)
    True

    Test - check instantiation with invalid arguments
    >>> Square(0)
    Traceback (most recent call last):
    ...
    ValueError: width must be > 0

    Test - check ``area`` method
    >>> s = Square(5)
    >>> s.area()
    25

    Test - check ``display`` method
    >>> s = Square(3, 1, 1)
    >>> s.display()
     ###
     ###
     ###

    Test - check ``__str__`` method
    >>> s = Square(4, 2, 3, 10)
    >>> str(s)
    '[Square] (10) 2/3 - 4'

    Test - check ``update`` method with positional arguments
    >>> s = Square(4, 1, 1, 10)
    >>> s.update(20, 5, 3, 4)
    >>> str(s)
    '[Square] (20) 3/4 - 5'

    Test - check ``update`` method with keyword arguments
    >>> s = Square(4, 1, 1, 10)
    >>> s.update(size=7, y=8, id=30)
    >>> str(s)
    '[Square] (30) 1/8 - 7'

    Test - check ``to_dictionary`` method
    >>> s = Square(4, 1, 1, 10)
    >>> s.to_dictionary()
    {'id': 10, 'size': 4, 'x': 1, 'y': 1}
