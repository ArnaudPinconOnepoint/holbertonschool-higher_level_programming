"The ``Rectangle`` module
==========================

Using ``Rectangle``
-------------------

First import ``Rectangle`` from the ``rectangle`` file:

    >>> Rectangle = __import__('rectangle').Rectangle

Tests:

    Test - check instantiation with valid arguments
    >>> r = Rectangle(5, 10)
    >>> isinstance(r, Rectangle)
    True

    Test - check instantiation with invalid arguments
    >>> Rectangle(0, 10)
    Traceback (most recent call last):
    ...
    ValueError: width must be > 0

    Test - check ``area`` method
    >>> r = Rectangle(5, 10)
    >>> r.area()
    50

    Test - check ``display`` method
    >>> r = Rectangle(3, 3)
    >>> r.display()
    ###
    ###
    ###

    Test - check ``__str__`` method
    >>> r = Rectangle(4, 5, 1, 2, 10)
    >>> str(r)
    '[Rectangle] (10) 1/2 - 4/5'

    Test - check ``update`` method with positional arguments
    >>> r = Rectangle(4, 5, 1, 2, 10)
    >>> r.update(20, 5, 6, 3, 4)
    >>> str(r)
    '[Rectangle] (20) 3/4 - 5/6'

    Test - check ``update`` method with keyword arguments
    >>> r = Rectangle(4, 5, 1, 2, 10)
    >>> r.update(width=7, y=8, id=30)
    >>> str(r)
    '[Rectangle] (30) 1/8 - 7/5'

    Test - check ``to_dictionary`` method
    >>> r = Rectangle(4, 5, 1, 2, 10)
    >>> r.to_dictionary()
    {'id': 10, 'width': 4, 'height': 5, 'x': 1, 'y': 2}
